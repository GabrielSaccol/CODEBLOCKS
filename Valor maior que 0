#include <stdio.h>
int main(){
   int n = 10, aux = -9999;
   printf ("Digite números\n");
      do{
        scanf("%d",&n);
        if(n>aux){
            aux = n;
        }

      }while(n!=0);
      printf("O maior valor digitado: %d\n", aux);

return 0;
}
